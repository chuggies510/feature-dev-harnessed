# Feature development - active work in progress (gitignored)
dev/features/active/

# Legacy dotfile artifacts (pre-unification)
.feature-dev/active/

# Legacy root-level artifacts (for backwards compatibility)
feature_list.json
claude-progress.txt
init.sh

# Note: dev/features/completed/ is NOT ignored - archived features are committed as documentation
